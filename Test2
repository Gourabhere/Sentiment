import React, { useState, useEffect } from 'react';
import HighchartsReact from 'highcharts-react-official';
import Highcharts from 'highcharts';

function SentimentChart() {
    const [chartOptions, setChartOptions] = useState({});

    useEffect(() => {
        // Fetch sentiment data from your API endpoint
        fetch('/api/sentiment')
            .then(response => response.json())
            .then(data => {
                // Process the data to calculate sentiment counts
                let negativeCount = 0;
                let neutralCount = 0;
                let positiveCount = 0;

                data.forEach(item => {
                    if (item.sentimentScore < -1) { // Adjust thresholds as needed
                        negativeCount++;
                    } else if (item.sentimentScore > 1) { // Adjust thresholds as needed
                        positiveCount++;
                    } else {
                        neutralCount++;
                    }
                });

                 // Calculate total count
                const totalCount = negativeCount + neutralCount + positiveCount;

                // Determine overall sentiment based on counts
                let overallSentiment = 'Neutral';
                if (positiveCount > negativeCount && positiveCount > neutralCount) {
                    overallSentiment = 'Overwhelmingly Positive'; // Or adjust as needed
                } else if (negativeCount > positiveCount && negativeCount > neutralCount) {
                    overallSentiment = 'Overwhelmingly Negative'; // Or adjust as needed
                }

                // Prepare data for the line chart
                const chartData = [
                    { name: 'Negative', y: negativeCount, color: '#dc3545' },
                    { name: 'Neutral', y: neutralCount, color: '#ffc107' },
                    { name: 'Positive', y: positiveCount, color: '#28a745' }
                ];

                // Set chart options for a line chart
                setChartOptions({
                    chart: {
                        type: 'line',
                        backgroundColor: '#f8f9fa' // Light background like the image
                    },
                    title: {
                        text: 'COMMUNITY SENTIMENT',
                        align: 'left'
                    },
                     subtitle: {
                        text: overallSentiment,
                        align: 'left',
                        style: {
                            fontWeight: 'bold',
                            fontSize: '18px'
                        }
                    },
                    xAxis: {
                        categories: ['Sentiment'],
                        title: {
                            text: null
                        },
                        labels: {
                            enabled: false
                        }
                    },
                    yAxis: {
                        title: {
                            text: 'Count'
                        },
                        min: 0,
                    },
                    series: [{
                        name: 'Sentiment',
                        data: chartData.map(item => item.y),
                        colorByPoint: true,
                        colors: chartData.map(item => item.color),
                        dataLabels: {
                            enabled: true,
                            format: '{point.y}',
                            style: {
                                fontSize: '14px'
                            }
                        }
                    }],
                    legend: {
                        enabled: false
                    },
                    plotOptions: {
                        line: {
                            dataLabels: {
                                enabled: true
                            },
                            enableMouseTracking: false
                        }
                    },
                     credits: {
                        enabled: false // Remove Highcharts credits
                    },
                    tooltip: {
                        enabled: false // Disable tooltips
                    }
                });
            })
            .catch(error => {
                console.error('Error fetching sentiment data', error);
            });
    },);

    return (
        <div>
            <HighchartsReact highcharts={Highcharts} options={chartOptions} />
        </div>
    );
}

export default SentimentChart;
 
